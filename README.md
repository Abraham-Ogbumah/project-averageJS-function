# project-averageJS-function
The Javascript function uses the ...rest parameter to accept an infinite number of values and return the average. When no parameter is passed to the function, zero is returned.
